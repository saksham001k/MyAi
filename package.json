{
  "name": "myai-autonomous-engine",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "myai": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc && node -e \"require('node:fs').cpSync('dist/src', 'dist', {recursive:true})\"",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "chalk": "^5.4.1",
    "commander": "^13.1.0",
    "execa": "^9.6.0",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.8"
  }
}
